# Jaspar Schultz — Software Engineering Portfolio

Personal software engineering portfolio showcasing engineering automation, developer tooling, full-stack products, security tooling, and systems work.

## Featured work

- Sheet2Schedule — C#/.NET Revit automation
- Volta — programming language/compiler project
- AI automation and MCP/Revit experimentation
- LaunchKit Pro — reusable frontend component product
- Scry — Python OSINT/security tooling
- QA Automation Showcase — Playwright/TypeScript
- Integration Lab — Laravel integration sandbox
- SendForge — Ruby on Rails application

## Local preview

This is a static site. You can open `index.html` directly, or serve the folder locally:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

## GitHub Pages

This repository includes `.github/workflows/deploy-pages.yml` and deploys the static site with GitHub Actions.

For the account-level site, name the GitHub repository:

`V3lCryn.github.io`

Then in GitHub go to **Settings → Pages → Build and deployment → Source → GitHub Actions**.

Every push to `main` will redeploy the portfolio.
